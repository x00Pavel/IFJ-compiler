#include <stdlib.h>
#include <string.h>
#include <stdio.h>

#include "scaner.h"

char * dynamic_string(const char *src){
    size_t string_len = strlen(src);

    char *stirng = (char *) malloc(string_len);    

    return  ;
}
